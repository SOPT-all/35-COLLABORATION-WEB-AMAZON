import { BASE_URL, END_POINT } from './api';
import { images, productCardImages } from './images';
import products from './mocks/products';
import { mockRecentSearches } from './mocks/recentSearches';

export { products, images, productCardImages, mockRecentSearches, BASE_URL, END_POINT };
